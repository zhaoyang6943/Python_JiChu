上节课复习
    1. for循环
        for x in [1,2,3]:
            print(x)

        for 与 while 循环对比：
        相同之处：都是循环，for循环能干的事，while循环都能干
        不同之处：
            for循环次数取决于in后值的个数
            while循环次数取决于条件何时为假

        for + range

        for + else

        for + break

        for + continue

        for i in range(len(l)):
            print(i,l[i])

        range()=>在python3中相当于老母鸡！python2就会很消耗内存！

    2. 基本数据类型
        4个方面去介绍知识点
        3个角度就归类
        作用:
        定义:
        类型转换:
        内置方法:

        第一个角度，是存一个值还是多个值；容器类型，非容器类型
        第二个角度，有序的还是无序的？索引！序列类型
        第三个角度，可变还是非可变！


    3. 今天内容：
        基本数据类型
            list
            tuple（不可变的列表）
            dict